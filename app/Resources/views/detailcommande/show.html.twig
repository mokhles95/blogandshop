{% extends 'base.html.twig' %}

{% block body %}
    <h1>Detailcommande</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detailCommande.id }}</td>
            </tr>
            <tr>
                <th>Qte</th>
                <td>{{ detailCommande.qte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detailcommande_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detailcommande_edit', { 'id': detailCommande.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
